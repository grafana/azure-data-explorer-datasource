name: Verify i18n markup has been committed

permissions:
  contents: read

on:
  pull_request:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ startsWith(github.ref, 'refs/pull/') }}

jobs:
  verify-i18n:
    if: (github.event_name == 'pull_request' || (github.event_name == 'push' && github.repository == 'grafana/azure-data-explorer-datasource'))
    uses: grafana/grafana-github-actions/.github/workflows/verify-i18n.yml@26e65a1da9ffdf35d3e8cc6dbeffbccf16022d88
